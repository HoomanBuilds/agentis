# ASCII Waitlist Rebuild Plan

## Design

1. Reuse the Nexus hero structure, green animated ASCII wave, pixel display type, black surfaces, mono labels, and subtle grid.
2. Remove the hero statistics and all hero actions so the opening section stays focused on the message.
3. Reduce the website to three substantial sections, following the Dike reference's sparse full-screen rhythm.
4. Keep the public identity name-neutral and display the official Botchain wordmark.

## Implementation

1. Add local ASCII wave, torus, and marketplace card components adapted from the supplied archive.
2. Rebuild the home page around hero, marketplace, and final signup sections.
3. Restyle the waitlist form and keep it exclusively in the final section.
4. Replace rejected identity copy in metadata, webhook source, documentation, and package metadata.
5. Preserve the existing waitlist API, bot trap, route restrictions, and webhook behavior.

## Validation

1. Run ESLint and a production build.
2. Capture desktop, tablet, and mobile screenshots.
3. Check overflow, form states, focus treatment, and reduced motion.
4. Commit the implementation in logical parts without pushing.
