# Botchain Waitlist Redesign Plan

1. Preserve and publish the existing application on `development`.
2. Remove previous product routes and API handlers from `main`.
3. Replace the application shell with a lightweight waitlist-only page.
4. Add a validated webhook-backed waitlist endpoint.
5. Update configuration examples and repository documentation.
6. Run lint, production build, route checks, and responsive visual review.
7. Commit the migration in logical parts and publish `main`.
